<?xml version="1.0" encoding="utf-8"?>
<!-- Paleta de comandos (Ctrl+P) — campo de filtro en vivo + lista de comandos filtrable, mismo
     patrón visual que la barra de búsqueda del sidebar (ver layout_sidebar.xml) pero sin
     duplicar su lógica: el filtro acá corre sobre CommandRegistry.filter, no FileTreeAdapter. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/ide_surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/studio_ic_search"
            android:contentDescription="@null" />

        <EditText
            android:id="@+id/command_palette_filter_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/command_palette_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:singleLine="true"
            android:textColor="@color/ide_text_primary"
            android:textColorHint="@color/ide_text_secondary"
            android:textSize="15sp" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/ide_divider" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/command_palette_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false" />

    <TextView
        android:id="@+id/command_palette_empty_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="@string/command_palette_empty"
        android:textColor="@color/ide_text_secondary"
        android:textSize="13sp"
        android:visibility="gone" />

</LinearLayout>
